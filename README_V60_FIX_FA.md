# نسخه V60 FIX

این نسخه مشکل صفحه سفید/باز نشدن ورود ادمین کامپیوتر را رفع می‌کند.

علت مشکل نسخه قبلی: کد CSS تم سه‌بعدی اشتباهاً داخل رشته JavaScript خروجی Word قرار گرفته بود و کل اسکریپت قبل از اجرا خطای Syntax می‌داد؛ بنابراین صفحه ورود ادمین نمایش داده نمی‌شد.

اصلاحات:
- بازسازی `app/index.html` از سورس سالم V57
- اعمال تم 3D به شکل امن داخل `<style>` اصلی
- اضافه شدن تب «حساب و امنیت» برای ادمین
- اضافه شدن تب «حساب کاربری» برای کلاینت
- تغییر نام کاربری و رمز عبور برای ادمین و کلاینت
- جایگزینی فایل HTML داخل mobile-admin و mobile-client
- تست Syntax با Node.js

ورود اولیه ادمین:
- username: admin
- password: admin123
